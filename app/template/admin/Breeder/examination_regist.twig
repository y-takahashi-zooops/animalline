{#
This file is part of EC-CUBE

Copyright(c) EC-CUBE CO.,LTD. All Rights Reserved.

http://www.ec-cube.co.jp/

For the full copyright and license information, please view the LICENSE
file that was distributed with this source code.
#}
{% extends '@admin/default_frame.twig' %}

{% set menus = ['breeders', 'breeders_breeder_list'] %}

{% block title %}{{ '審査結果登録'|trans }}{% endblock %}
{% block sub_title %}{{ 'ブリーダー管理'|trans }}{% endblock %}

{% block stylesheet %}
    <link rel="stylesheet" href="{{ asset('assets/css/tempusdominus-bootstrap-4.min.css', 'admin') }}">
{% endblock stylesheet %}

{% block javascript %}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/js/bootstrap.bundle.min.js"></script>

<script>
    $('#examination_result').change(function () {
        result = document.getElementById('examination_result').value

        if( result ==  1){
            $('#examination_result_comment').attr('disabled', true);
        } else {
            $('#examination_result_comment').attr('disabled', false);
        }
    });

    document.getElementById("confirm").onclick = function() {
        $('#modal').modal('show');
    };

    $("#confirm-yes").on('click', function (e) {
        document.getElementsByName('form-examination-regist')[0].submit();
    });
</script>
{% endblock javascript %}

{% block main %}
<form name="form-examination-regist" role="form" class="form-horizontal" id="form-examination-regist" method="post" action="">
{# {{ form_widget(form._token) }} #}
    <div class="c-contentsArea__cols">
        <div class="c-contentsArea__primaryCol">
            <div class="c-primaryCol">
                <div class="card rounded border-0 mb-4">
                    <div class="card-header"><span class="card-title">審査結果編集</span></div>
                    <div class="card-body">
                        <div class="row mb-2">
                            <div class="col-2">
                                <span>審査結果</span>
                            </div>
                            <div class="col-2 ec-select">
                                <select id="examination_result" name="examination_result" class="form-control">
                                    {% set results = ['許可', '拒否'] %}
                                    {% for i in 1..2 %}
                                        <option value="{{ i }}" {{ examination.examinationResult == i ? 'selected' : '' }}>{{ results[i - 1] }}</option>
                                    {% endfor %}
                                </select>
                            </div>
                        </div>
                        <div class="row mb-2">
                            <div class="col-2">
                                <span>メール本文</span>
                                <span class="badge badge-primary ml-1">{{ 'admin.common.required'|trans }}</span>
                            </div>
                            <div class="col-10">
                                <textarea id="examination_result_comment" name="examination_result_comment" class="form-control" rows="15"
                                    {{ examination.examinationResult != 2 ? 'disabled' : '' }}
                                    >{{ examination.examinationResult != 2 ? '審査に関する情報のお問い合わせにはお答えできません' : examination.examinationResultComment }}</textarea>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="c-conversionArea" id="aside_column">
        <div class="c-conversionArea__container">
            <div class="row justify-content-between align-items-center">
                <div class="col-6">
                    <div class="c-conversionArea__leftBlockItem">
                        <a class="c-baseLink" href='{{ path('admin_breeder_examination', {'id': examination.breeder.id}) }}'>
                            <i class="fa fa-backward" aria-hidden="true"></i><span>審査情報表示</span>
                        </a>
                    </div>
                </div>
                <div class="col-6">
                    <div id="ex-conversion-action" class="row align-items-center justify-content-end">
                        <div class="col-auto">
                            <button class="btn btn-ec-conversion px-5" id="confirm" type="button">{{ 'admin.common.confirm'|trans }}</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>
{{ include('animalline/components/_modal_mail_confirm.twig') }}
{% endblock %}