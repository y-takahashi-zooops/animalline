{% set site_type = 'breeder' %}
{% extends 'animalline/breeder/frame.twig' %}

{% block main %}
    <section id="breeder-column" class="breeder-column_dog">
        <div class="inner">
            <div class="breeder-column__main_section_title">
                <h3>
                    掲載中のペット
                </h3>
            </div>
            <div class="breeder-column__main_section_body">
                <ul>
                    {% for pet in pets %}
                        <li>
                            <a href="{{ url('breeder_pets_edit', {'id': pet.id}) }}">
                                <figure>
                                    <img src="{{ app.request.schemeAndHttpHost ~ '/' ~ constant('Customize\\Config\\AnilineConf::ANILINE_IMAGE_URL_BASE') ~ (pet.thumbnailPath | default('/noimage_plugin_list.png')) }}" class="card-img-top" alt="Pet-image"></figure>
                                </a>
                                <h4>
                                    {{ pet.getBreedsType().getBreedsName() ?? '' }}
                                </h4>
                                <div class="birthday">
                                    {{ pet.petBirthday|date('Y年m月d日') }}
                                    生                                  
                                </div>
                                <div class="price">
                                    {{ pet.price|number_format(0) }}
                                    円（税込）                                  
                                </div>
                                <a href={{ path('breeder_pets_edit', {'id': pet.id} )  }} class="btn w-100 btn-warning text-align-center my-1">
                                    編集                                        
                                </a>
                            </li>
                        {% else %}
                            <span class="mb-2">
                                結果が０件です。
                            </span>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>
    </section>
{% endblock %}

{% block stylesheet %}
    <link rel="stylesheet" href="{{ asset('assets/css/breeder-style.css') }}">
{% endblock %}
{% block javascript %}{% endblock %}