{% set site_type = 'breeder' %}
{% extends 'animalline/breeder/frame.twig' %}
{% set status = 3 %}

{% block main %}
<<<<<<< HEAD
    <div class="page-inner">
        <div class="breadcrumbs">
            <ul>
                <li><a href="/">アニマルラインTOP</a></li>
                <li><i class="bi bi-chevron-right"></i>マイページ</li>
            </ul>
        </div>
    
        <div id="mypage-menu">
            <h1><i class="bi bi-chat-right-quote-fill"></i>取引メッセージ</h1>
        </div>
    
        <div class="mypage-service_list">
            <div class="mypage-service_list__content">
                <div class="mypage-service_list__content_body">
                    <ul class="message" style="background: #FFF">
                        {% for message in listMessages %}
                            {% set status = null %}
                            <li>
                                <a href="{{ url('breeder_message', {'id': message.id}) }}">
                                    <div class="image">
                                        <figure>
                                            <img src="{{ app.request.schemeAndHttpHost ~ '/' ~ constant('Customize\\Config\\AnilineConf::ANILINE_IMAGE_URL_BASE') ~ (message.pet.thumbnailPath ?? '/noimage_plugin_list.png') }}" alt="{{ message.pet.breedsType.breedsName ?? '' }}">
                                        </figure>
                                        <div class="image-icon">
                                        {{ message.pet.petSex == 1 ? '<span class="image-icon_male">男の子</span>' : '<span class="image-icon_female">女の子</span>' }}
                                        </div>
                                    </div>
                                    <div class="text">
                                        <div class="name">
                                            <h3>{{ message.breeder.breederName ?? '' }}</h3>
                                        </div>
                                        <div class="profile">
                                            <ul>
                                                <li>{{ message.pet.breedsType.breedsName ?? '' }}</li>
                                                <li>{{ message.pet.petBirthday.format('Y/m/d') ?? '' }} 生まれ</li>
                                            </ul>
=======
    <div class="main">
        <div class="row">
            <div class="main-container col-12">
                <h1>取引メッセージ一覧</h1>
                <div class="m-3 p-3"></div>
                {% for message in listMessages %}
                    <div class="row border breeder-border rounded bg-white p-2 mb-3">
                        <div class="col-12">
                            <a href="{{ url('breeder_message', {'id': message.Id}) }}">
                                <div class="row">
                                    <div class="col-md-2 col-3" style="display: grid; place-items: center;">
                                        {# <h6 class="p-2 rounded" style="background:#539b34; color:#fff;">
                                            {% set status = ['交渉中', '成約確認待ち', '成約', '非成約'] %}
                                            {{ status[message.contractStatus] }}
                                        </h6> #}
                                        <img src="{{ app.request.schemeAndHttpHost ~ '/' ~ constant('Customize\\Config\\AnilineConf::ANILINE_IMAGE_URL_BASE') ~ (message.pet.thumbnailPath | default('/noimage_plugin_list.png')) }}"
                                            class="card-img-top" alt="Pet-image" style="max-width:100px; border-radius:100px;">
                                        <div class="type"><span style="font-weight:300">{{ message.pet.petSex == constant('Customize\\Config\\AnilineConf::ANILINE_PET_SEX_MALE') ? '男の子' : '女の子' }}</span></div>
                                    </div>
                                    <div class="col-md-10 col-9">
                                        <div class="row">
                                            <div class="col-7">
                                                {# <h5>{{ message.contactTitle ?? '' }}</h5> #}
                                                <h5>{{ message.Breeder.breederName }}ブリーダー</h5><br>
                                                <span style="font-weight:300">{{ message.pet.breedsType.breedsName}}</span><br>
                                                <span style="font-weight:300">{{ message.pet.petBirthday|date('Y/m/d') }} 生まれ</span>
                                            </div>
                                            <div class="col-5">
                                                <h6 class="text-right pb-1">
                                                    {% for item in  message.breederContacts %}
                                                        {% if item.messageFrom == constant('Customize\\Config\\AnilineConf::MESSAGE_FROM_MEMBER') %}
                                                            {% set status = item.isReading %}
                                                        {% endif %}
                                                    {% endfor %}
                                                    {% if status == constant('Customize\\Config\\AnilineConf::RESPONSE_REPLIED') %}
                                                        <span data-toggle="tooltip"
                                                          title="既読" class="d-inline-block normal-tooltip"
                                                          tabindex="0">
                                                            <i class="fas fa-envelope-open"></i>
                                                        </span>
                                                    {% elseif status == constant('Customize\\Config\\AnilineConf::RESPONSE_UNREPLIED') %}
                                                        <span data-toggle="tooltip"
                                                              title="未読" class="d-inline-block red-tooltip"
                                                              tabindex="0">
                                                            <i class="fas fa-envelope"></i>
                                                        </span>
                                                    {% endif %}
                                                </h6>
                                                <h6 class="text-right">
                                                    {# 返信日時：{{ message.lastMessageDate.format('Y年m月d日 H:i') ?? 'なし' }} #}
                                                    {{ message.lastMessageDate.format('Y/m/d H:i') ?? 'なし' }}
                                                    <br>
                                                    {{ message.customerNewMsg ? '未読あり' }}
                                                </h6>
                                            </div>
>>>>>>> 05d9fb53d8e31ee12398832855230e3ac60ebedb
                                        </div>
                                    </div>
                                    <div class="receive">
                                        <div class="receive-icon">
                                            {% if (message.breederContacts|length==0) and (message.contractStatus is not constant('Customize\\Config\\AnilineConf::CONTRACT_STATUS_NONCONTRACT')) %}
                                                <i class="bi bi-envelope"></i>
                                            {% endif %}
                                            {% for item in message.breederContacts %}
                                                {% if item.messageFrom == constant('Customize\\Config\\AnilineConf::MESSAGE_FROM_MEMBER') %}
                                                {% set status = item.isReading %}
                                                {% endif %}
                                            {% endfor %}
                                            {% if status == constant('Customize\\Config\\AnilineConf::RESPONSE_REPLIED') %}
                                                <i class="bi bi-envelope-open"></i>
                                            {% elseif (status is not null) and (status == constant('Customize\\Config\\AnilineConf::RESPONSE_UNREPLIED')) %}
                                                <i class="bi bi-envelope"></i>
                                                <span class="budge">1</span>
                                            {% endif %}
                                        </div>
                                        <p>{{ message.lastMessageDate.format('Y/m/d H:i') ?? 'なし' }}</p>
                                    </div>
                                </a>
                            </li>
                        {% else %}
                            結果が０件です。
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>
    
        {#
        <div class="mypage-footer">
            <div class="polocy">
                <figure><img src="/html/template/default/assets/img//mypage/privacy_d.gif" alt="プライバシーマーク" /></figure>
                <p>〇〇〇〇は、個人情報の適切な管理をするプライバシーマーク付与事業者として認定されています。</p>
            </div>
        </div>
        #}
    </div>
{% endblock %}
{% block stylesheet %}
    <link rel="stylesheet" href="{{ asset('assets/css/breeder-style.css') }}">
    <link rel="stylesheet" href="{{ asset('assets/css/mypage.css') }}">
{% endblock %}