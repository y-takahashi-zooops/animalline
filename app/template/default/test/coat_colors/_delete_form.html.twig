<form method="post" action="{{ path('coat_colors_delete', {'id': coat_color.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coat_color.id) }}">
    <button class="btn">Delete</button>
</form>
